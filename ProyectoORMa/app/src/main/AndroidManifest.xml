<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jonathan.proyectoorma">

    <application
        android:name=".config.myAplication"
        android:allowBackup="true"
        android:icon="@mipmap/mobile"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="AA_DB_NAME"
            android:value="dba"/>
        <meta-data
            android:name="AA_MODELS"
            android:value="com.example.jonathan.proyectoorma.modelo.producto"/>
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="1"/>

        <activity android:name=".actividadPrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>